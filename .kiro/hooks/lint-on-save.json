{
  "version": "v1",
  "hooks": [
    {
      "name": "Lint on Save",
      "trigger": "PostFileSave",
      "description": "Runs npm run lint whenever a JS or JSX file is saved",
      "matcher": "\\.(js|jsx)$",
      "action": {
        "type": "command",
        "command": "npm run lint"
      }
    }
  ]
}